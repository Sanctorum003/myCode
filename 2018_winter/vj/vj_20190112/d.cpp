//关键点:一个位置如果重复访问必然冗余，所以bfs时要排除掉已访问过的位置
#include <iostream>
#include <queue>
using namespace std;
#define Num 10003

bool a[Num] = {0};

int main(void)
{
    int x,y;
    cin>>x>>y;
    queue<int> q;
    
    return 0;
}
