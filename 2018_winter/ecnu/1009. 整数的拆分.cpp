#include <iostream>
#include <algorithm>
using namespace std;

int main(void)
{
    int n;
    while(cin>>n)
    {
        int dp[n+1];
        fill(dp,dp+n+1,1);
        dp[0]=0;
        for(int i = 1; i<=n;++i)
        {
            dp[n]
        }
    }
    return 0;
}