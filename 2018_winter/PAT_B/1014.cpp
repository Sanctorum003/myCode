#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
    vector<string> v;
    vector<char> c;
    string s;
    int m = 4;
    while(m--)
    {
        cin>>s;
        v.push_back(s);
    }

    int i = 0;
    int j = 0;  
    while(i < s[0].size() && j < s[0].size())
    {
        
    }
    return 0;
}